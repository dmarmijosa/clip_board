# Nombre de dominio al que apuntará el despliegue (sin protocolo)
APP_DOMAIN=clipboard.nexa-code.net

# Email para los certificados TLS automáticos de Caddy (Let's Encrypt)
LETSENCRYPT_EMAIL=operaciones@nexa-code.net

# Credenciales de PostgreSQL
POSTGRES_DB=clipboard
POSTGRES_USER=clipboard
POSTGRES_PASSWORD=cambia-esta-contraseña

# Configuración SSH para deploy.sh
SSH_USER=deploy
SSH_HOST=server.example.com
SSH_PORT=22
REMOTE_DIR=/var/www/clipboard
